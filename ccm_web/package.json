{
  "name": "ccm_web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "nodemon --config nmon_config.json server/app.ts",
    "build-client": "cd client && npm run build",
    "compile-server": "tsc",
    "preprod": "npm run build-client && npm run compile-server",
    "prod": "NODE_ENV=production node server/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.31",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  }
}
